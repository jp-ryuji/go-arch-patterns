# External Ports
export POSTGRES_PORT_EXTERNAL=5433
export REDIS_PORT_EXTERNAL=6380

# Database Configuration
export POSTGRES_HOST=postgres
export POSTGRES_PORT=5432
export POSTGRES_USERNAME=user
export POSTGRES_PASSWORD=password
export POSTGRES_DBNAME=mydb

# Redis Configuration
export REDIS_HOST=redis
export REDIS_PORT=6379
export REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}

# Constructed Database URL
export DATABASE_URL="postgresql://${POSTGRES_USERNAME}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DBNAME}"
